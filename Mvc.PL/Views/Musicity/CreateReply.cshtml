@model Mvc.PL.ViewModels.CreateReplyPage.CreateReplyPageReplyViewModel

@{
    ViewBag.Title = "Add new Reply";
}

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Add new Reply</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Content)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Content)
            @Html.ValidationMessageFor(model => model.Content)
        </div>

        <p>
            <input type="submit" value="Submit" style="background-color: #333; border: 1px solid black; color: White; padding: 0.4em 1em; text-decoration: none;"/>
        </p>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}