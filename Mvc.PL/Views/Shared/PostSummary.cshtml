@model Mvc.PL.ViewModels.TopicPage.TopicPagePostViewModel

    <div class="editor-label">
        @Html.RouteLink(Model.Name, new { controller = "Musicity", action = "Replies", post = Model.Name })
    </div>